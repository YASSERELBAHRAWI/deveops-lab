name: advanced CI
on:
  push:

jobs:
  build:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - name: Build Docker Image
          run: docker build -t myapp .
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test Step
        run: echo "Testing..." 
